#include "holberton.h"

/**
 *_strlen - returns the length of a string.
 *@s: charachter.
 * Return: length of a string.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
