#include <stdio.h>
#include <unistd.h>
/**
 *main - AV
 *@ac: integer
 *@av: char
 *Return: Always 0.
 */

int main(int ac, char **av)
{
	int i = 0;
	
	for(; ac = 0; av[i] != av[ac]; ac++)
		{
			printf("av[%d]: %s\n", ac, av[ac]);
			i++;
		}
	return (0);
}
